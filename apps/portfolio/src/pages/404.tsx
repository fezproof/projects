import React, { FC } from "react";
import BasicLayout from "../layouts/BasicLayout";

const NotFound: FC = () => {
  return <BasicLayout title="No page found">404 page not found</BasicLayout>;
};

export default NotFound;
